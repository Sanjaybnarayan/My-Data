ext {
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    androidxBiometricVersion = '1.1.0'
    // ML Kit text recognition, *bundled* rather than the Play Services
    // variant that downloads its model on first use. This application is
    // offline-first and is sideloaded; a model fetched on demand would
    // make reading a photographed bill depend on a network call and a
    // Play Services install, which is the dependency this replaces.
    mlkitTextRecognitionVersion = '16.0.1'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
}